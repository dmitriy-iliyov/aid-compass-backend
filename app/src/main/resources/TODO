aid-compass-backend % docker build -f Dockerfile.jbr -t jbr-17.0.11 .

chmod +x build-and-run.sh

echo ">>>   del backend image if exist"
docker compose -f infrastructure/docker-compose.yaml -p aid-compass down

echo ">>>   rebuild "
docker build -f Dockerfile.jbr -t jbr-17.0.11 infrastructure/

ip based firewall